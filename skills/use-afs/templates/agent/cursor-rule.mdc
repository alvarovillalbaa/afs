---
description: Use AFS as the shared context and source-of-truth structure.
alwaysApply: true
---

# AFS Cursor Rule

- Read `AGENTS.md`, `BRAIN.md`, `VISION.md`, `LOOPS.md`, `TASTE.md`, and
  `GAPS.md` when relevant before changing behavior, product direction,
  knowledge, or subjective quality.
- Put external source material in `sources/`.
- Do not create `official-documentation/`.
- Put live facts in `facts/items/<domain>/`, `facts/episodes/<domain>/`, or
  `facts/triples/<domain>/`, and technical how-to guides in `cookbooks/`.
- Keep large root-style Markdown files out of legacy `context/`.
- Add unresolved knowledge or understanding gaps to `GAPS.md`.
- Prefer updating durable AFS files over burying reusable context in chat.
